# tests/ — layout, final-spec entry points, and provenance

Reorganized from a flat 56-file directory into dataset/role
subpackages (this commit; `git log --follow` preserves each file's
history). Pre-registration documents (#1–#4) cite the original flat
paths together with frozen commit SHAs — those paths are valid at
those SHAs; current paths are below.

## Final-spec entry points (reproduce the results)

| Evidence | Command | Result it reproduces |
|---|---|---|
| NAB self-calibrated (paper §6.1–6.2) | `python -m tests.nab.benchmark_nab_selfcal` | unknown 85%@56/10k, combined 96.3%@303 |
| NAB corpus + protocol audit (§6.3–6.4) | `python -m tests.nab.benchmark_nab_corpus --aggregate` | anchored 33.40, sweep inflation +25–59 |
| NAB one-class baselines (§6.3) | `python -m tests.nab.benchmark_nab_baselines` | OC-SVM/iForest/LOF under the identical harness |
| SKAB / TEP (§6.5) | `python -m tests.multivariate.benchmark_skab` / `benchmark_tep` | 100%@382/10k; T² degeneration |
| Duel / costs / transfer (§6.5) | `python -m tests.multivariate.exp_support_duel` / `exp_deployability` / `exp_frozen_transfer` | parity+deltas; 468 FLOPs; FAR-with-detection transfer table |
| Synthetic probes (§6.5–§7) | `python -m pytest tests/probes -q` | ghost state, worked consumer, H2, SPE/T² identities, contextual gate |
| Core unit tests | `python -m pytest tests/core -q` | unknown channel, OR calibration, cycle-phase mechanism |
| Manuscript figures (v1 / Appendix) | `python -m tests.figures.make_figures` | doc/figures/fig1–fig5 |
| **Paper results snapshot (v2 build step)** | `python -m tests.figures.export_paper_results` | paper_results/*.csv, *.npz, manifest.json — re-executes the five pre-registered computations via the frozen runners (identical seeds/splits/order) and verifies key stats against the registered numbers |
| Manuscript figures (v2 main text) | `python -m tests.figures.make_paper_v2_figures` | doc/figures/v2_fig1–v2_fig5, generated from paper_results/ ONLY (no dataset access) |
| Hydraulic exploration | `python -m tests.hydraulic.exp_hydraulic` | doc/explorations/hydraulic_exploration.md |
| Paderborn #1 | `python -m tests.paderborn.exp_paderborn_full` | doc/preregistrations/experiment_plan_paderborn.md §5 |
| Paderborn #2 | `python -m tests.paderborn.exp_paderborn2` | doc/preregistrations/experiment_plan_paderborn2.md §8 |
| Paderborn #3 | `python -m tests.paderborn.exp_paderborn3` | doc/preregistrations/experiment_plan_paderborn3.md §7 |
| IMS + Milling #4 | `python -m tests.ims.exp_ims` | doc/preregistrations/experiment_plan_ims.md §4 |
| Hydraulic #5 | `python -m tests.hydraulic.exp_hydraulic_prereg` | doc/preregistrations/experiment_plan_hydraulic.md §7 |

## Directory map

| Dir | Role | Status |
|---|---|---|
| `core/` | detector-core unit tests (unknown regime, combined calibration, scorer statelessness, cycle-phase mechanism) | maintained; run in CI-style checks |
| `probes/` | synthetic mechanism probes with paper-cited assertions (ghost, downstream policy, contextual gate, H2, MSPC identities) + mode-change probe | paper evidence (path-frozen in manuscript) |
| `baselines/` | shared baseline implementations: PCA-SPE/T² (`mspc_baselines`), FGMM-BIP reconstruction (`fgmm_bayes`, dual reading disclosed) | paper evidence |
| `nab/` | NAB loaders (`nab_datasets`, `nab_features`, `nab_metrics`) + the three protocol harnesses + the original per-file sweep harness (`benchmark_nab`, protocol P2) | paper evidence |
| `multivariate/` | SKAB/TEP loaders + benchmarks + contextual stratification + the three §6.5 experiments | paper evidence |
| `hydraulic/` | UCI 447 loader + exploration (granularity ladder, cycle-phase recovery) + pre-registration #5 runner | exploration + pre-registered (SHA-anchored) |
| `paderborn/` | Paderborn loader + subset exploration + pre-registrations #1/#2/#3 runners | pre-registered (SHA-anchored) |
| `ims/` | NASA IMS + Milling loaders + pre-registration #4 runner | pre-registered (SHA-anchored) |
| `figures/` | manuscript figure generation (validated palette, regenerates every data panel) | paper support |
| `legacy/` | pre-NSAD era harnesses (ablation v1/v2, changepoint/sparse/drift benchmarks, NAB v1–v6 progression harnesses, synthetic generators, GPU equivalence, `test_split_equivalence` — requires the removed `lambda3_detector_v2`) | inventory per architecture.md §13.8; not on any current evidence path |

## Data directories (all gitignored; loaders expect repo root)

| Dir | Source |
|---|---|
| `NAB/` | github.com/numenta/NAB |
| `SKAB/` | github.com/waico/SKAB |
| `TEP/` | github.com/camaramm/tennessee-eastman-profBraatz |
| `HYDRAULIC/` | UCI id 447 via github.com/Machine-Learning-FGA/Hydraulic-systems (CC BY 4.0) |
| `PADERBORN/` | Paderborn KAt Data Center (CC BY-NC 4.0) via this repo's GitHub Release `pu-bearing-data` |
| `IMS/`, `MILLING/` | NASA Prognostics Data Repository (phm-datasets.s3.amazonaws.com), datasets 4 and 3 |
| `nab_score_cache/` | regenerated by `benchmark_nab_corpus` compute phase |

Known pre-existing failure: `legacy/test_split_equivalence.py` imports
`lambda3_detector_v2`, which no longer exists — kept as inventory, not
collected into any current suite.
